<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asus.activiteas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.AboutActivity" />
        <activity android:name=".Activity.StartActivity" />
        <activity android:name=".Activity.PresetActivity" />
        <activity android:name=".Activity.ListActivity" />
        <activity android:name=".Activity.AddProductActivity" />
        <activity android:name=".Activity.DeleteActivity" />
        <activity android:name=".Activity.ListOfProdActivity" />
        <activity android:name=".Activity.EnterNameOfListActivity"></activity>
    </application>

</manifest>